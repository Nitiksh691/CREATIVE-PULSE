# M.O.N.K.Y OS - System Architecture

## 📐 Complete System Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER BROWSER                             │
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        │ HTTP Requests
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│                    NEXT.JS APP ROUTER                           │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │              MIDDLEWARE.TS                              │   │
│  │  • Check authentication (Clerk)                         │   │
│  │  • Verify onboarding status                             │   │
│  │  • Protect routes                                       │   │
│  │  • Redirect unauthorized users                          │   │
│  └────────────────────────────────────────────────────────┘   │
│                        │                                         │
│                        ▼                                         │
│  ┌─────────────┬──────────────┬───────────────┬─────────────┐ │
│  │   Public    │  Protected   │      API      │  Webhooks   │ │
│  │   Routes    │    Routes    │    Routes     │             │ │
│  └─────────────┴──────────────┴───────────────┴─────────────┘ │
│       │              │               │               │          │
│       │              │               │               │          │
└───────┼──────────────┼───────────────┼───────────────┼──────────┘
        │              │               │               │
        ▼              ▼               ▼               ▼
┌──────────────────────────────────────────────────────────────────┐
│                     ROUTE HANDLERS                                │
│                                                                   │
│  PUBLIC ROUTES:          PROTECTED ROUTES:                       │
│  • /                     • /dashboard                            │
│  • /jobs                 • /profile                              │
│  • /companies            • /settings                             │
│  • /creator-discover     • /applications                         │
│  • /auth/login           • /company-dashboard                    │
│  • /auth/register        • /onboarding                           │
│                                                                   │
│  API ROUTES:             WEBHOOK ROUTES:                         │
│  • /api/user/onboarding  • /api/webhooks/clerk                   │
│  • /api/jobs             •   (auto-sync users)                   │
│  • /api/applications     •                                       │
└──────────────┬────────────────────────────┬──────────────────────┘
               │                            │
               │                            │
               ▼                            ▼
┌────────────────────────────┐   ┌─────────────────────────────┐
│      CLERK AUTH            │   │     MONGODB DATABASE        │
│  • User authentication     │   │                             │
│  • Session management      │◄──┤  ┌─────────────────────┐   │
│  • JWT tokens              │   │  │   USER COLLECTION   │   │
│  • OAuth providers         │   │  │  • clerkId          │   │
│  • Webhook events          │   │  │  • email            │   │
└────────────────────────────┘   │  │  • role             │   │
                                  │  │  • onboarding       │   │
                                  │  │  • profile data     │   │
                                  │  └─────────────────────┘   │
                                  │                             │
                                  │  ┌─────────────────────┐   │
                                  │  │   JOB COLLECTION    │   │
                                  │  │  • title            │   │
                                  │  │  • description      │   │
                                  │  │  • company (ref)    │   │
                                  │  │  • skills           │   │
                                  │  │  • status           │   │
                                  │  └─────────────────────┘   │
                                  │                             │
                                  │  ┌──────────────────────┐  │
                                  │  │ APPLICATION COLLECT. │  │
                                  │  │  • job (ref)         │  │
                                  │  │  • freelancer (ref)  │  │
                                  │  │  • company (ref)     │  │
                                  │  │  • status            │  │
                                  │  │  • cover letter      │  │
                                  │  └──────────────────────┘  │
                                  └─────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### **1. User Registration & Onboarding**

```
┌─────────┐
│  USER   │
└────┬────┘
     │
     │ 1. Clicks "Join"
     ▼
┌──────────────────┐
│ /auth/register   │
└────┬─────────────┘
     │
     │ 2. Fills form
     ▼
┌──────────────────┐
│  CLERK SIGNUP    │◄───────┐
└────┬─────────────┘        │
     │                      │
     │ 3. Creates account   │
     ▼                      │
┌──────────────────┐        │
│  CLERK WEBHOOK   │        │
└────┬─────────────┘        │
     │                      │
     │ 4. Webhook fires     │
     ▼                      │
┌──────────────────┐        │
│   MONGODB        │        │
│ (User created)   │        │
└────┬─────────────┘        │
     │                      │
     │ 5. Redirect          │
     ▼                      │
┌──────────────────┐        │
│  /onboarding     │        │
└────┬─────────────┘        │
     │                      │
     │ 6. Select role       │
     │ 7. Fill details      │
     ▼                      │
┌──────────────────┐        │
│ POST /api        │        │
│ /user/onboarding │        │
└────┬─────────────┘        │
     │                      │
     │ 8. Save to DB        │
     ▼                      │
┌──────────────────┐        │
│   MONGODB        │        │
│ (Profile updated)│        │
└────┬─────────────┘        │
     │                      │
     │ 9. Update metadata   │
     ├──────────────────────┘
     │
     │ 10. Redirect
     ▼
┌──────────────────┐
│   /dashboard     │
└──────────────────┘
```

### **2. Company Posts Job**

```
┌──────────┐
│ COMPANY  │
└────┬─────┘
     │
     │ 1. Goes to dashboard
     ▼
┌────────────────────┐
│ /company-dashboard │
└────┬───────────────┘
     │
     │ 2. Clicks "Post Job"
     ▼
┌────────────────────┐
│  Job Form Opens    │
└────┬───────────────┘
     │
     │ 3. Fills job details
     │ 4. Submits
     ▼
┌────────────────────┐
│ POST /api/jobs     │
└────┬───────────────┘
     │
     │ 5. Validate auth
     │ 6. Check role = company
     ▼
┌────────────────────┐
│    MONGODB         │
│  (Job saved)       │
└────┬───────────────┘
     │
     │ 7. Job appears
     ▼
┌────────────────────┐
│    /jobs (Public)  │
└────────────────────┘
```

### **3. Freelancer Applies to Job**

```
┌──────────────┐
│  FREELANCER  │
└──────┬───────┘
       │
       │ 1. Browse jobs
       ▼
┌──────────────────┐
│     /jobs        │
└──────┬───────────┘
       │
       │ 2. Click job
       ▼
┌──────────────────┐
│  /jobs/[id]      │
└──────┬───────────┘
       │
       │ 3. Click "Apply"
       ▼
┌──────────────────────┐
│ Application Form     │
└──────┬───────────────┘
       │
       │ 4. Fill details
       │ 5. Submit
       ▼
┌─────────────────────────────┐
│ POST /api/applications      │
└──────┬──────────────────────┘
       │
       │ 6. Check auth
       │ 7. Check role = freelancer
       │ 8. Prevent duplicate
       ▼
┌─────────────────────────────┐
│       MONGODB               │
│  • Create application       │
│  • Increment job.appCount   │
└──────┬──────────────────────┘
       │
       │ 9. Notify company
       ▼
┌─────────────────────────────┐
│   Company sees in dashboard │
└─────────────────────────────┘
```

---

## 🗄️ Database Schema Relationships

```
┌──────────────────────────┐
│         USER             │
│ ──────────────────────── │
│ _id: ObjectId            │
│ clerkId: string ◄────────┼──────┐
│ email: string            │      │
│ role: enum               │      │ References
│ onboardingCompleted: bool│      │
└──────────┬───────────────┘      │
           │                      │
           │ Creates              │
           │                      │
┌──────────▼───────────────┐      │
│         JOB              │      │
│ ──────────────────────── │      │
│ _id: ObjectId            │      │
│ title: string            │      │
│ company: ObjectId ───────┼──────┘
│ status: enum             │
│ skills: string[]         │
└──────────┬───────────────┘
           │
           │ Has many
           │
┌──────────▼──────────────────┐
│      APPLICATION            │
│ ──────────────────────────  │
│ _id: ObjectId               │
│ job: ObjectId ──────────────┼─┐
│ freelancer: ObjectId ───────┼─┼─┐
│ company: ObjectId ──────────┼─┼─┼─┐
│ status: enum                │ │ │ │
│ coverLetter: string         │ │ │ │
└─────────────────────────────┘ │ │ │
                                │ │ │
       References: ─────────────┘ │ │
         Job ─────────────────────┘ │
         User (Freelancer) ─────────┘
         User (Company)
```

---

## 🔐 Security Layers

```
┌────────────────────────────────────────────────┐
│  LAYER 1: CLERK AUTHENTICATION                 │
│  • JWT token validation                        │
│  • Session management                          │
│  • OAuth security                              │
└───────────────┬────────────────────────────────┘
                │
                ▼
┌────────────────────────────────────────────────┐
│  LAYER 2: MIDDLEWARE PROTECTION                │
│  • Route access control                        │
│  • Onboarding verification                     │
│  • Role-based redirects                        │
└───────────────┬────────────────────────────────┘
                │
                ▼
┌────────────────────────────────────────────────┐
│  LAYER 3: API ROUTE VALIDATION                 │
│  • User role verification                      │
│  • Request payload validation                  │
│  • Business logic checks                       │
└───────────────┬────────────────────────────────┘
                │
                ▼
┌────────────────────────────────────────────────┐
│  LAYER 4: DATABASE CONSTRAINTS                 │
│  • Unique indexes                              │
│  • Required fields                             │
│  • Data type validation                        │
└────────────────────────────────────────────────┘
```

---

## 🚀 Request Lifecycle

```
1. User Makes Request
   ↓
2. Middleware Intercepts
   ├─ Is public route? → Allow
   ├─ Is authenticated? → Check onboarding
   └─ Not authenticated? → Redirect to /auth/login
   ↓
3. Route Handler Executes
   ├─ Page component renders
   └─ API route processes
   ↓
4. Database Queries
   ├─ MongoDB connection (cached)
   ├─ Query execution
   └─ Results returned
   ↓
5. Response Sent to Client
   ├─ HTML/JSX rendered
   └─ JSON data returned
```

---

This architecture ensures:
✅ Scalability - Stateless middleware, cached DB connections
✅ Security - Multi-layer validation
✅ Performance - Indexed queries, optimized data flow
✅ Maintainability - Clear separation of concerns
